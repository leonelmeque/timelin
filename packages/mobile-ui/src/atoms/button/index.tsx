import React from 'react';
import { View, Text } from 'react-native';
import { tokens, hooks } from '@todo/commons/dist';

export const Button = () => (
  <View style={{ backgroundColor: 'blue' }}>
    <Text>Normal Button</Text>
  </View>
);
